---
title: プロファイル管理
description: Hermes Desktopのプロファイル機能。モデル・スキル・.env・SOUL.md（ペルソナ）をまとめたプロファイルの作成、切り替え、マルチプロファイル運用。
slug: dashboard/profiles
sidebar:
  order: 1
tags:
  - hermes
  - dashboard
  - profiles
audience:
  - Hermes Desktop利用者
status: verified
hermes_version: 0.17.0
hermes_commit: 857d024
verified: '2026-06-20'
---

> [!note] 確認状況
> v0.17 実機（ビルド `857d024`）のプロファイルパネルで確認。表示値（モデル名・スキル数など）は環境依存の例。

プロファイルは、モデル・スキル構成・`.env`・ペルソナ（SOUL.md）をひとまとめにした「構成のセット」。用途ごとにプロファイルを切り替えて使える（例: 既定 / コーディング用 / ローカル非公開用 / リサーチ用 / ワーカー用）。

## プロファイル一覧

左側にプロファイルが並ぶ。各プロファイルにはスキル数と `env` が表示される。「＋ 新しいプロファイル」で追加する。選択すると右側に詳細が出る。

| 項目 | 内容 |
|---|---|
| 名前 | プロファイル名（既定プロファイルには「デフォルト」バッジ） |
| `.env` | プロファイルごとの環境変数ファイル |
| パス | 設定の保存先（既定は `~/.hermes`） |
| モデル | 使用モデルとプロバイダー（例: `grok-4.3 · xai-oauth`） |
| スキル | 有効なスキル数 |

## SOUL.md（システムプロンプト / ペルソナ）

「SOUL.MD」欄に、そのプロファイルに組み込まれるシステムプロンプトとペルソナの指示を書く。編集して「SOUL を保存」で保存する。プロファイルごとに人格・方針を変えられる。

## セットアップをコピー

「セットアップをコピー」を押すと、そのプロファイルを別環境で再現するためのセットアップコマンドがクリップボードにコピーされる。共有先で実行すれば同じ構成を再現できる。

## プロファイルの切り替え

[[02_基本操作/03_キーボードショートカット|キーボードショートカット]]で素早く切り替えられる。

- 既定プロファイル: `⌘ D`
- プロファイル 1〜9: `⌘ 1` 〜 `⌘ 9`
- プロファイル 10〜18: `⌘ ⌥ 1` 〜 `⌘ ⌥ 9`
- 次 / 前のプロファイル: `⌘ ⇧ ]` / `⌘ ⇧ [`

> [!note] ブラウザのプロファイルビルダー
> v0.17 ではブラウザのダッシュボードからもプロファイルを構築できる（モデル・スキル・MCP を選ぶ）。本ページは Desktop アプリ内のプロファイル管理を扱う。差分は[[06_リファレンス/04_変更履歴|変更履歴]]を参照。

## 前提・操作・確認

- 前提: 少なくとも 1 つのプロファイル（既定）が存在する。
- 操作: 「新しいプロファイル」で名前と複製元（既定 / なし / 既存プロファイル）を指定して作成する。作成後、モデル・スキルは設定画面やブラウザのビルダーで、SOUL.md は「SOUL.MD」欄で設定する。用途ごとに切り替える。
- 期待結果: 選んだプロファイルのモデル・スキル・ペルソナでセッションが動く。
- 注意: プロファイルごとに `.env`（API キー等）が分かれる。キーが未設定だと対象プロバイダーのモデルは使えない。

## 関連

- [[03_設定/01_モデル|モデル]]
- [[07_ダッシュボード/02_スキルとツール|スキルとツール]]
- [[03_設定/09_プロバイダー|プロバイダー]]
